import * as React from "react";

const Textarea = (): JSX.Element => (
    <form action="">
        <textarea rows={30} cols={50} id="messageText" autoComplete="off">
        </textarea>
    </form>
)

export default Textarea